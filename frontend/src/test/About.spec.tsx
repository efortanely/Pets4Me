import React from "react";
import { mount, shallow } from "enzyme";
import { expect } from "chai";

// author Connor
describe("About", () => {
  it("should hello world", () => {
    expect(1).to.equal(1);
  });
});
